<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="10dp">

    <RelativeLayout
        android:id="@+id/relativeLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/shape_kategori"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.cardview.widget.CardView
            android:id="@+id/asd"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_margin="10dp"
            android:outlineSpotShadowColor="@color/white">

            <ImageView
                android:id="@+id/iv_profile_member_admin"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:scaleType="centerCrop"
                android:src="@drawable/user_pic" />
        </androidx.cardview.widget.CardView>

        <TextView
            android:id="@+id/tv_nama_member_admin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignTop="@+id/asd"
            android:layout_marginTop="0dp"
            android:layout_toEndOf="@+id/asd"
            android:text="Kamera"
            android:textColor="@color/blue_background"
            android:textSize="18sp" />

        <TextView
            android:id="@+id/tv_username_member_admin"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/tv_nama_member_admin"
            android:layout_marginTop="5dp"
            android:layout_toEndOf="@+id/asd"
            android:text="12 Items" />

    </RelativeLayout>

</androidx.constraintlayout.widget.ConstraintLayout>